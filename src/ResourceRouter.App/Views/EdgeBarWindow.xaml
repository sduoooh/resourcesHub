<Window x:Class="ResourceRouter.App.Views.EdgeBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:ResourceRouter.App.Views"
    xmlns:state="clr-namespace:ResourceRouter.App.State"
        Title="EdgeBar"
    Width="{x:Static state:EdgeBarLayoutDefaults.CollapsedHostWidthDip}"
        Height="{x:Static state:EdgeBarLayoutDefaults.WindowHeightDip}"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="True"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        PreviewKeyDown="OnWindowPreviewKeyDown"
        Background="Transparent">
    <Grid x:Name="RootGrid"
          Background="Transparent"
          AllowDrop="True"
          DragEnter="OnRootDragEnter"
          DragOver="OnRootDragOver"
          DragLeave="OnRootDragLeave"
          Drop="OnRootDrop">
        <Border x:Name="SensorStrip"
            Width="{x:Static state:EdgeBarLayoutDefaults.SensorStripWidthDip}"
                HorizontalAlignment="Right"
            Background="#FF2A3440"
            BorderBrush="#FF8DA5C0"
                BorderThickness="1"
                CornerRadius="5"
                MouseLeftButtonDown="OnSensorMouseLeftButtonDown"
                MouseMove="OnSensorMouseMove"
                MouseLeftButtonUp="OnSensorMouseLeftButtonUp"
                MouseRightButtonUp="OnSensorMouseRightButtonUp" />

        <Popup x:Name="DropPopup"
             Placement="Absolute"
               StaysOpen="True"
               AllowsTransparency="True"
               PopupAnimation="Fade">
            <Border x:Name="DropPopupBorder"
                                        AllowDrop="True"
                                        DragEnter="OnRootDragEnter"
                                        DragOver="OnRootDragOver"
                                        DragLeave="OnRootDragLeave"
                                        Drop="OnRootDrop"
                    BorderBrush="#FF454E58"
                    BorderThickness="1"
                    CornerRadius="12"
                    Background="#FF141A1F"
                    Padding="8">
                <Border.RenderTransform>
                    <ScaleTransform x:Name="DropPopupScale" ScaleX="0.96" ScaleY="0.96" />
                </Border.RenderTransform>
                <views:DropPanel x:Name="DropPanelControl" />
            </Border>
        </Popup>

        <Popup x:Name="CardListPopup"
             Placement="Absolute"
               StaysOpen="True"
               AllowsTransparency="True"
               PopupAnimation="Fade">
            <Border x:Name="CardListPopupBorder"
                                        AllowDrop="True"
                                        DragEnter="OnRootDragEnter"
                                        DragOver="OnRootDragOver"
                                        DragLeave="OnRootDragLeave"
                                        Drop="OnRootDrop"
                    Width="{x:Static state:EdgeBarLayoutDefaults.CardListPopupWidthDip}"
                Height="{x:Static state:EdgeBarLayoutDefaults.CardListPopupHeightDip}"
                MinHeight="{x:Static state:EdgeBarLayoutDefaults.CardListPopupVisualMinHeightDip}"
                MaxHeight="{x:Static state:EdgeBarLayoutDefaults.CardListPopupMaxHeightDip}"
                    Background="#FF12171C"
                    BorderBrush="#FF3A4450"
                    BorderThickness="1"
                    CornerRadius="12">
                <Border.RenderTransform>
                    <ScaleTransform x:Name="CardListPopupScale" ScaleX="0.96" ScaleY="0.96" />
                </Border.RenderTransform>
                <DockPanel>
                    <views:ManualInputCard x:Name="ManualInputControl" DockPanel.Dock="Top" Visibility="Collapsed" />
                    <views:CardListPanel x:Name="CardListControl" />
                </DockPanel>
            </Border>
        </Popup>
    </Grid>
</Window>